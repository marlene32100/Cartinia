import Page from "classes/Page";

export default class Sold extends Page {
  constructor() {
    super({
      id: "sold",
      element: ".sold__wrapper",
      elements: {},
    });
  }

  create() {
    super.create();
  }
}
